{
  "name": "@bonnard/content",
  "version": "0.1.0",
  "private": true,
  "description": "Shared documentation content for Bonnard",
  "scripts": {
    "build": "echo 'Static content — no build needed'"
  }
}
